#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    i = get_string("Text: ");

    int l = 0
    while (i[0] !=  "\0" || "." || "!" || "?")
    {
        l++
    }
    return l;

    s =

    Grade Level = 0.0588 * l - 0.296 * s = 15.8
    printf("%i", Grade Level)
}
