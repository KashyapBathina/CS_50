#include <stdio.h>
#include <cs50.h>

int main(void)
{
    (n)
}

void answer(int n);
{
    int n;
    do
    {
        n = get_int("How tall is the pyramid?  ");
    }
    while ( n > 8 || n < 1);

    for ( i = 0; i < 8; i++)
        printf("#\n");
}
