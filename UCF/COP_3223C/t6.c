#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <math.h>

/*------------module 6------------------------------------------------------------------------------------------------------------*/

/*
Arra:data struc that holds series of variables with the same datatype
    Gen Struct: type var name[size];
                char colors[5];
                start array with 0 and goes to 4
                int numbers[10];
                numbers[0] = 1;
                numbers[1] = 2;
    Common Mistakes:
                1. Out of bounds: happends when your index into array is invalid when yu try to reference it
                2. 


*/

int main() {


}