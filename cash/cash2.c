#include <stdio.h>
#include <cs50.h>
int cents(void)
int main(void)
{
    float dollar
    do
    {
    int cents = get_int("Amount owed\n");
    }
    while ( cents <= 0)

    int coins = 0

    for (int cents > 0)
        {
            int quarters = calculate_quarters(n)
            n = n - quarters
        }
}